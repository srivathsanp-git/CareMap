# CareMap environment variables — copy to .env.local and fill in.
#
# Census ACS API key (free): https://api.census.gov/data/key_signup.html
# Enables the live "poverty" SDOH metric. Without it the app still runs fully on
# live CDC PLACES data and uses the bundled snapshot value for poverty.
VITE_CENSUS_KEY=
